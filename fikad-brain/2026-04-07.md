
### DS

##### 어제 한 일
hooksnap
- Woodpecker 이메일 템플릿 셋업 + 최초 10명 유튜버 캠페인 실행
- 블로그 자동화 — GitHub CI + loop skill 셋업
- 랜딩 페이지 리디자인 스펙 작성

##### 오늘 할 일
- 정란님 미팅
hooksnap
- AI 파이프라인 개선 — ai-test 앱으로 썸네일 위 텍스트 생성 + 타이틀 별도 생성 파이프라인 개선
- 템플릿 강제 적용 시 썸네일 품질 스코어링 개선 (TG-525)
- 콜드 아웃리치 캠페인: 게이밍 유튜버 v1 (TG-530)
- 갱신 알림 Resend 이메일 추가 (TG-553)

---

personal
- 심야 책방 강의 준비 — AI 책 선정 + 서평 + 타겟/기획 (carry-over)

etc
- 인프런 강의 — section 5~끝 학습 내용 정리 + Notion 이관 (carry-over)

##### 오늘의 workflow update
- spec 파일에서, 구체적으로 plan 파일까지 만들게 되면, 이게 어떤 파일들을 수정할지 알게 되므로, 티켓 간 merge conflict 를 미리 체크할 때 좋고, 그렇다면 parallel implementation을 편하게 돌릴 수 있게 된다.

---

### Materials
### Skill Update


##### human ops

 Here's a summary of all 9 Awaiting Human Ops tickets, what's needed, and
  what you should do:

  ---
  High Impact — Unblocks Other Tickets

  1. TG-450 — Before/After Image Generation

  Blocks: TG-451, TG-454 (SEO pages)
  What's needed: You provide 1-2 YouTube URLs per niche (gaming, tech, vlog,
   education, music, podcasts, cooking, fitness) so the pipeline can
  generate real before/after sample thumbnails.
  Action: Pick 8-16 YouTube video URLs (one per niche) and comment them on
  the ticket. Claude can then run the generation script.

  2. TG-510 — Langfuse title evaluation system

  Blocks: TG-512 (E2E title hooks verification)
  What's needed: Set up Langfuse account and add credentials to ai-test/.env
   (LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST).
  Action: Create a Langfuse account (free tier), create a project, copy the
  API keys into ai-test/.env. Or if you want to skip Langfuse, this can be
  marked Done since dry-run mode already works.

  3. TG-468 — PubSubHubbub subscription service

  Blocks: TG-469 (lease renewal)
  What's needed: Add PUBSUB_HUB_SECRET env var to Render, run DB migration,
  update privacy policy.
  Action: This ticket is gated — description says "Do not implement until
  Phase 1 achieves >15% digest→generation conversion within 6 weeks."
  Consider if this should just be deferred/cancelled rather than blocking
  the pipeline.

  ---
  Deploy Verification — Quick Wins

  4. TG-475 — Engagement pipeline verification

  What's needed: Run initial classification task, verify endpoints, check
  admin dashboard.
  Action: This is a verification ticket. I can handle most of it if you want
   — just say "verify TG-475" and I'll run the checks.

  5. TG-387 — Weekly Pulse KPI API

  What's needed: Add POSTHOG_API_KEY (server-side REST API key) to Render
  web service. Verify POLAR_ACCESS_TOKEN exists.
  Action: Go to Render → web service → Environment → add POSTHOG_API_KEY.
  Get the key from PostHog → Settings → Project API Keys.

  ---
  Outreach Campaign — Lower Priority

  6. TG-457 — Woodpecker API client + quality gate

  What's needed: WOODPECKER_API_KEY env var on Render (already provisioned
  per CLAUDE.md).
  Action: If the Woodpecker key is already set on Render, this might be
  ready to verify. Check Render env vars.

  7. TG-460 — Cold outreach email templates

  What's needed: Human review of email copy, test send to verify inbox
  delivery.
  Action: Review the emails in Woodpecker, send test emails to yourself,
  verify they land in inbox (not spam).

  ---
  Stale — Consider Closing

  8. TG-437 — Zone bleed fix comparison test (last updated Apr 1)

  What's needed: Manual visual inspection — run AI pipeline variants,
  compare screenshots, select winner.
  Action: This requires hands-on visual quality comparison. Either schedule
  time to run the comparison, or defer if zone bleed is no longer a
  priority.

  9. TG-392 — Affiliate page partner email capture (last updated Mar 26)

  What's needed: Set up partner@hooksnap.io email address (or redirect).
  Action: Create the email in Google Workspace, or set up a redirect from
  partner@ to your inbox. Then mark Done.

  ---
  Recommended Priority Order

  10. TG-392 — 2 min, just create email redirect → Done
  11. TG-510 — 5 min, create Langfuse account or mark Done (dry-run works) →
  unblocks TG-512
  12. TG-387 — 5 min, add PostHog API key to Render → Done
  13. TG-475 — 10 min, let me run the verification
  14. TG-450 — 15 min, pick YouTube URLs per niche → unblocks TG-451 → TG-454
  15. TG-468 — Defer (gated on metrics threshold)
  16. TG-437 — Defer or schedule dedicated time
  17. TG-457/460 — Only if outreach campaign is still active

  Want me to handle any of the verification-type tickets (TG-475, TG-457)
  while you work through the quick wins?

