

- transcript를 읽고, 어떤 내용들을 가르칠지를 기록한다.
- 아마 이 작업을 10번 정도 하면, 각 섹션당 가르칠 내용이 많이 나오지 않을까 싶다. 그러면 그것들을 바탕으로 다시 transcript 작성. 이 작업을 3번 정도 반복. 그러면 가르칠 것들과 가르치지 않을 것들의 흐름이 나옴.

```
Here are more feedbacks:

- Overall, the lecture plan feels like an offline one-day class.  
  For "lecture-studio" skills, I want it to make a full online       
  course, not the offline course. Could you update the skills, to     
  match with this intention? 
  
- I want each section to follow the stroyline of hook -> explain -> crisis -> solution. This is similar to the storyline of short-form videos. Can you plan this? Currently, I like the section format, that suggests the problem, fix them, and wrap up. But I want to suggest "hook" first(students might dying to learn it, etc...)

- I think there are too detailed code explanation and skill writing. I don't want the students to open up the editor, and read the code. This course is not for that. I want the students to understand the app in more architectural way. Still, they need to understand the technologies they used, architecture they need to choose, the technical tradeoffs they need to make. But I will make CLAUDE.md, any hooks, any rules, or codes with claude code. For example, I'll prompt to claude code saying "I want to make a hook that checks if the PLAN.md is approved, when build related command runs. make plan-approval-check.sh and write it."

- when each session's ended, i want to teach students to add a SessionEnd hook that runs wrapping up the session, update CLAUDE.md and related files. I also want to make this into skill so that I could call it whenever I want. Is that possible?
  
- in section 4, there are an explanation that in "SessionStart" hook it reinjects some contexts. But, the claude code already does reinjectnig CLAUDE.md when compaction. Could you explain what's the difference between putting "reinject-context.sh" texts, and just writing them in CLAUDE.md from the first place?

- For formatting or blocking the file edit/write/delete, I think hook use looks good, but hooks are pretty limited for usage, because it's not allowed to define multiple hooks. I need more inspiration for hook to teach better/useful examples. Could you tell me more?
```

- Write/Edit 시에 formatting hook 넣는 것 좋을 듯.
- git worktree - 
- subagent inline 으로 mcp 설정하는 것 너무 좋다.
	- claude code context 에 등록하지 않고, subagent 만 넣는다.
	- 이거 테스트해보자.

```
`e2e-tester.md` 파일을 만듭니다.

```markdown
---
name: e2e-tester
description: Runs E2E tests using Playwright browser automation
mcpServers:
	playwright:
		command: npx
		args: ["@playwright/mcp@latest"]
---
``
```

---

```
here are more feedbacks:

- Overall, the lectures are keep making hooks, and hook building is the main part. But, I don't want the hook building to be the center of this lecture, but the actual ai workflows that can be run by skill triggering.
```

- `SessionStart` hook 에서는 dynamic hook 을 넣을 수 있다. `git log --oneline -5` 로 최근 commit을 가져오거나, `cat STATUS.md` 로 최근 상태를 가져오거나 등등. jira tickets 을 가져오거나, 어떤 state를 작업하고 있었는지 등등. 그렇다면, 세션이 끝났을 때 `SessionEnd` hook 에서는 어떤 작업까지 마무리 했는지를 한 번 더 체크하고 session을 마친다거나 하면 좋지 않을까.
- 뭐 그밖에, 위험한 파일을 지우거나 하지 말아야 함.
- `disable-model-invocation` 을 구체적으로 지정하는 것들 너무 좋음. 어떤 스킬을 구체적으로 트리거할건지에 대한 고민이 좋음.